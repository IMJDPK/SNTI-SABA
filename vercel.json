{
  "buildCommand": "cd frontend && rm -rf node_modules .vite && npm install && npm run build",
  "outputDirectory": "frontend/dist",
  "framework": "vite",
  "installCommand": "npm install --prefix frontend --legacy-peer-deps"
}
